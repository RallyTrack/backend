spring:
  datasource:
    url: jdbc:h2:mem:rallytrack;MODE=MariaDB;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: test-only-secret-that-is-long-enough-for-hs256

cloud:
  aws:
    s3:
      bucket: rallytrack-test
      endpoint: ""
      public-endpoint: ""
      ai-endpoint: ""
      path-style: true
    region: us-east-1
    credentials:
      access-key: test-access-key
      secret-key: test-secret-key

app:
  analysis:
    callback-secret: test-callback-secret
